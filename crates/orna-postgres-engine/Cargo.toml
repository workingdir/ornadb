[package]
name = "orna-postgres-engine"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "Apache-2.0"
build = "../orna-postgres/build.rs"

[features]
default = []
embedded = []

[dependencies]
