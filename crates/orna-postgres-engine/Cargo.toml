[package]
name = "orna-postgres-engine"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "Apache-2.0"

[features]
default = []
embedded = ["dep:orna-postgres", "orna-postgres/embedded"]

[dependencies]
orna-postgres = { path = "../orna-postgres", optional = true }
