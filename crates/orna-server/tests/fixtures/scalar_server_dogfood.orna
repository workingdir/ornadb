CREATE SCHEMA scalar_server_dogfood;

CREATE SERVER FUNCTION scalar_server_dogfood.echo(p_value INTEGER)
RETURNS INTEGER
SECURITY INVOKER
TRANSACTION READ ONLY
VOLATILITY STABLE
AS
    SELECT p_value;
