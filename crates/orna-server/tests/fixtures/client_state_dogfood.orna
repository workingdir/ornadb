CREATE SCHEMA state_fixture;

CREATE CLIENT FUNCTION state_fixture.scalar() RETURNS BOOLEAN IS
    STATE local_flag BOOLEAN SCOPE LOCAL DEFAULT TRUE;
    STATE session_flag BOOLEAN SCOPE SESSION DEFAULT NULL;
    STATE user_flag BOOLEAN SCOPE USER;
BEGIN
    RETURN TRUE;
END;
