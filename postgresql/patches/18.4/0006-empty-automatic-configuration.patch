diff -U0 -Nr a/src/bin/initdb/initdb.c b/src/bin/initdb/initdb.c
--- a/src/bin/initdb/initdb.c
+++ b/src/bin/initdb/initdb.c
@@ -277,0 +278 @@
+static void create_empty_autoconf(void);
@@ -1464,0 +1466,13 @@
+create_empty_autoconf(void)
+{
+	char		path[MAXPGPATH];
+	char	  **lines = pg_malloc_array(char *, 1);
+
+	lines[0] = NULL;
+	snprintf(path, sizeof(path), "%s/postgresql.auto.conf", pg_data);
+	writefile(path, lines);
+	if (chmod(path, pg_file_create_mode) != 0)
+		pg_fatal("could not change permissions of \"%s\": %m", path);
+}
+
+static void
@@ -1628,14 +1641,0 @@
-	/* postgresql.auto.conf */
-
-	conflines = pg_malloc_array(char *, 3);
-	conflines[0] = pg_strdup("# Do not edit this file manually!\n");
-	conflines[1] = pg_strdup("# It will be overwritten by the ALTER SYSTEM command.\n");
-	conflines[2] = NULL;
-
-	sprintf(path, "%s/postgresql.auto.conf", pg_data);
-
-	writefile(path, conflines);
-	if (chmod(path, pg_file_create_mode) != 0)
-		pg_fatal("could not change permissions of \"%s\": %m", path);
-
-
@@ -3279,0 +3280 @@
+	create_empty_autoconf();
