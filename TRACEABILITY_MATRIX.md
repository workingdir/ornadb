# Work Traceability Matrix

This matrix separates accepted implementation status from proof status. A row
records an accepted slice when the implementation is present in the tree; it
does not by itself claim that the named test ran in this checkout.

- **Current validated focused evidence:** the `work/` checkout records the
  protocol, sealed-server, LSP UTF-16, no-`STATE` dogfood, resource-span,
  buffered-preflight, V3/V5 upgrade, same-revision replacement, selected-package
  check and formatter commands listed below; prior workspace-unit reports remain
  historical and were not rerun, while the current static editor-tooling gate is
  recorded below. The current static editor-tooling and Compose integration
  gates are recorded below. Clean-host, Neovim/Vim host sessions, manual
  Zed/VSIX runtime parity, live REF, same-major PostgreSQL, and remaining
  accepted contract gates remain pending or unclaimed.
  The accepted Qt v1 runtime/provider/package boundary, bounded populated
  Inspector resource/UI/presentation/runtime projections, V8 Rows and retained
  table presentation, and V9 structural UI constructors have implementation
  slices and focused local evidence below. The Compose integration matrix now
  passes the installed evaluator and Inspector-recursion paths. Installed
  Qt package selection, clean-host proof, V8/V9 continuation-specific
  persistence proof, reflective gateways, general Rows/object-value semantics,
  and remote transport remain pending or deferred; no unrun environment result
  is claimed.

## Current validated focused evidence (`work/` checkout)

- `cargo test -p orna-protocol sealed_event_batch_uses_event_tag_and_result_credit_lifecycle` — 1 test returned success.
- `cargo test -p orna-server --lib sealed_` — 4 tests returned success.
- `cargo test -p orna-lsp --test lsp_e2e serves_accepted_client_semantic_tokens_with_utf16_and_nested_ranges` — 1 test returned success after correcting the expected UTF-16 token range.
- `cargo test -p orna-server --test standard_database checks_and_evaluates_accepted_client_local_assignment_fixture_offline` — 1 test returned success after correcting the fixture test's durable function-identity conversion.
- `cargo test -p orna-compiler accepts_scalar_resource_assignment_await_with_exact_spans_and_call_provenance` — 1 test returned success.
- `cargo test -p orna-compiler discovers_stream_resource_target_with_resolved_element_type` — 1 test returned success.
- `cargo test -p orna-server buffered_sealed_cancel_prevents_acceptance_for_all_preflight_outcomes` — 1 test returned success.
- `cargo test -p orna-standard prepares_the_v3_to_v4_standard_upgrade_from_an_empty_v3_active_revision` — 1 test returned success.
- `cargo test -p orna-standard prepares_the_v5_to_v6_standard_upgrade_from_an_empty_v5_active_revision` — 1 test returned success.
- `cargo test -p orna-client same_revision_terminal_replacement_persists_when_new_evaluation_fails` — 1 test returned success.
- `cargo test -p orna-client same_revision_terminal_replacement_persists_when_later_expression_fails` — 1 test returned success.
- `cargo check -p orna-client -p orna-compiler -p orna-lsp -p orna-postgres -p orna-protocol -p orna-server -p orna-standard -p orna-syntax` — returned success with existing dead-code warnings.
- `cargo fmt --all -- --check` — returned success.
- Historical report (not current validation): a prior `cargo test --workspace --all-targets` run recorded 2545 tests, 0 failed, and 235 ignored across 40 suites; it was not rerun for this checkout, so no current workspace result is claimed.
- Historical report (not current validation): a later proof report recorded 2578 workspace tests and 14 LSP protocol tests plus 31 accepted tree-sitter cases; these checks were not rerun for this checkout and are not claimed as current passes.
- Historical report (not current validation): a prior `just editor-tooling-check` run recorded 14 LSP protocol tests and the 31-case tree-sitter corpus; that report was not rerun for this checkout. The current static gate result is recorded in the editor integrations row below.
- `cargo test -p orna-lsp --test lsp_e2e serves_final_field_name_through_accepted_rename_transition` — 1 final-field rename navigation test returned success; this focused proof is current. Zed ORDER BY highlight parity remains pending.
- `python3 scripts/check-editor-tooling.py` passed on 2026-08-27, parsed and
  validated 49 `.orna` files, and enforces the accepted Zed grammar revision
  `f5c9007ee2ba8dcd00784e806a9d9b32be6efe08`; a temporary alternate valid
  40-hex revision was rejected. The static check does not claim host sessions,
  Zed/VSIX runtime parity, or remote grammar-byte verification.
- Current selector-focused presenter targets are
  `crates/orna-postgres/src/kernel/server_execution.rs::sealed_output_qualified_standard_json_resolves_before_presenter_selection`,
  `sealed_output_qualified_application_type_without_presenter_preserves_requested_name`,
  `sealed_output_catalogue_collision_without_a_presenter_tie_stays_unresolved`,
  and `sealed_output_streaming_requirement_respects_non_streaming_presenters`;
  the recorded 355-test `cargo test -p orna-postgres --lib` pass predates the
  selector-only changes and is retained as historical context.
- `cargo test -p orna-server --lib source_check` passed after the static UI
  fixture test was added, including
  `crates/orna-server/src/source_check.rs::tests::checks_static_ui_source_fixture_against_verified_v9_standard`.
- `just runtime-qt-test` passed with the CMake build and ctest 1/1;
  `just runtime-qt-rust-smoke target/runtime-qt/liborna-runtime-qt.so` passed
  with canonical state 1191 bytes; and `just studio-qt-smoke
  target/runtime-qt/liborna-runtime-qt.so` passed with displayed canonical UI
  and shutdown.
- `just runtime-qt-package` passed; CPack generated
  `target/runtime-qt/packages/orna-runtime-qt-1.0.0-Linux.deb`. Installed
  package selection remains unproven.
- `just runtime-abi-header-check && just runtime-abi-parity` passed against the
  canonical ABI header and Rust mirror values.
- `cargo test -p orna-server --lib source_apply` passed with 17 tests.
- `cargo test -p orna-lsp` passed with 60 tests across 4 suites.
- `cargo test -p orna-postgres --lib recovery` passed with 39 tests.
- `cargo test -p orna-standard --lib` passed with 88 tests after V9 work;
- `cargo test -p orna-compiler --lib` passed with 471 tests; and
  `cargo test -p orna-client --lib` passed with 326 tests with existing warnings.
- `cargo test -p orna-core --lib` passed with 542 tests;
  `cargo test -p orna-server --lib` passed with 288 tests with existing
  warnings; and `cargo test -p orna-postgres --lib server_execution` passed
  with 90 tests with existing warnings.
- `cargo clippy -p orna-client --all-targets -- -D warnings` passed after the
  accepted error-layout and runtime-adapter cleanup; `cargo test -p orna-client
  --all-targets` passed with 326 tests across 3 suites.
- `cargo test -p orna-postgres --lib` passed with 356 tests and 3 ignored after
  safe lint cleanup commit `34e39f4`; package Clippy still has residual
  intentional error-layout and private-enum findings, so no workspace lint pass
  is claimed.
- `python3 scripts/run-demos.py` passed after excluding the unsupported generic
  scalar fixture; source-check and offline demos passed, while
  `client-inspector` was skipped because it is Compose-only. The source-authored
  `static-ui` demo at `crates/orna-server/tests/fixtures/static_ui_dogfood.orna`
  is accepted evidence; no excluded scalar fixture is accepted.
- `just kernel-test` passed with the dedicated temporary `ornadb_kernel_gate`
  database; final run artifact is `artifact://22571`. It covered 38
  `standard_database` tests, 5 user-state tests, 3 PostgreSQL library tests,
  53 `apply` tests, 20 `bootstrap` tests, 4 catalogue-health tests, 64
  recovery tests, 20 server-execution tests, and 22 server-mutation tests.
  The expected negative-path diagnostics were emitted by their tests; no
  failure was reported.

The initial LSP expectation mismatch, CLIENT fixture compile mismatch, and
workspace private-import compile failure were development corrections, not gate
failures. The current Compose/Docker matrix passed the ignored installed
evaluator, Inspector-recursion, source-apply, user-state, and PostgreSQL
integration suites. Clean-host, Neovim/Vim host sessions, manual Zed/VSIX
runtime parity, live REF, same-major PostgreSQL, and remaining contract results
remain pending or unclaimed.
- **Implementation present, proof pending:** current nested source changes are
  listed in the explicit section below.
- **Historical evidence (not current validation):** named focused symbols,
  reported counts, and nested commit IDs in the matrix are retained as prior
  provenance unless a section explicitly says current validated evidence.

A live proof in a test marked `#[ignore = "requires the Compose PostgreSQL development service"]`
is Compose-gated. The current `just kernel-test` run passed the ignored server
and PostgreSQL suites against per-test databases plus the dedicated empty
`ornadb_kernel_gate`; this matrix claims that recorded run only. Installed
Debian package selection, the fresh network-disabled host proof, Neovim/Vim
host sessions, manual Zed/VSIX runtime parity, live REF field-path evaluation,
the same-major PostgreSQL predecessor transition, and accepted contract gates
remain pending or unclaimed in `../TODO.md` and `PLAN.md`.


## Current accepted implementation boundaries (2026-08-27)

- ADRs 0082-0088 and 0090 are reflected in current source: Qt v1
  runtime/provider/package, bounded populated Inspector rows (including
  selected-sink carrier redaction), retained V8 Rows/table routing, V9
  structural UI constructors, generic CLIENT named-argument ordering, variable
  Rows-cell validation, local principal/session authority, and the registered
  V9 constructor showcase demo.
- Recent commits `f55df8d`, `fd21c1c`, `c68bb0a`, `b260ac5`, `50a0cfc`,
  `113012c`, `767991d`, `57c0bf9`, and `33aa485` record the accepted
  implementation and client quality/demo slices; `2f8509f`, `07ecc28`,
  `880ca17`, and `ca831a5` record evidence/boundary updates.
- Closed Beads boundaries are `ornadb-el1.3.2.1.1`,
  `ornadb-el1.5.13.9`, `ornadb-el1.5.14.2`, `ornadb-el1.5.14.7`,
  `ornadb-el1.3.4.3`, `ornadb-el1.4.4`, `ornadb-br0.2`, and
  `ornadb-br0.4.5`; broad security parent `ornadb-el1.3.4` remains open.
  Resource terminal provenance `ornadb-el1.2.1.43` is closed by ADR 0078/commit
  `767991d`; authenticated commit-receipt authority and producer evidence are
  reconciled.
- Generic no-FROM parameter projection `ornadb-el1.3.14` and scalar demo
  `ornadb-br0.4.4` remain blocked by ADR 0055: the closed
  `NoInputParameterSelect` AST/body is accepted only for fixed standard
  `std.invoke.echo` in `orna.std/2`; the canonical spec/standard-source
  contract does not accept generic parameter projection or generic scalar
  executable.


## Language and compiler

| Accepted slice | Implementation evidence | Focused local evidence | Installed or environment-gated proof |
|---|---|---|---|
| SERVER query and mutation functions | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-postgres` | Resolver and relational/mutation checker tests, including `accepts_a_checked_server_function_with_a_relational_plan` and `checks_server_insert_with_exact_body_identities_and_evidence`; historical provenance `1aeef47`: `crates/orna-server/tests/standard_database.rs::checks_and_prepares_server_function_dogfood_fixture_offline` checks four accepted SERVER functions, prepares against the offline base pair, and materialises the offline candidate active pair | `crates/orna-postgres/tests/server_execution.rs` and `server_mutation_execution.rs` are Compose-gated `#[ignore]` tests; no local installed proof is claimed |
| CLIENT Boolean, expression, state, resource, action, Inspector, and programmable control-flow forms (ADR 0084) | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-artifact`, `crates/orna-client` | Syntax fixtures and parser tests; client-plan versions 3 through 9 retain their historical compatibility and rejection coverage, while accepted programmable CLIENT plans use V10 (`crates/orna-artifact/src/client_plan.rs::CONTROL_FLOW_FORMAT_VERSION`); `crates/orna-compiler/src/resolver.rs::tests::lowers_ordinary_client_call_with_canonical_target_identities_and_reference`, `crates/orna-compiler/src/resolver.rs::tests::lowers_client_ref_field_path_concat_with_stable_identities_and_spans`, `crates/orna-client/src/lib.rs::client_expression_call_depth_is_bounded_by_artifact_limit`, `crates/orna-compiler/src/resolver.rs::tests::accepts_external_client_parameters_and_capabilities`, and `crates/orna-compiler/src/prepare.rs::tests::accepted_client_action_preparation_preserves_durable_operation_identity_and_arguments` retain the prior focused evidence; V10 implementation is exercised by the accepted `client_control_flow_dogfood.orna` source-check demo and the current package-suite reports below; historical provenance `6e61996`: `crates/orna-compiler/src/resolver.rs::rejects_v1_source_unit_identity_mutations` | Installed evaluator tests in `crates/orna-server/tests/standard_database.rs` are Compose-gated `#[ignore]` tests; live REF field-path evaluation remains blocked by the missing trusted object-loader seam, and no local Compose or live REF result is claimed |
| Declaration-selected execution domain; no `RUNS ON` syntax | `crates/orna-syntax`, `../spec/docs/02-status-decisions.md` | Syntax diagnostics and resolver domain checks | No separate installed proof is claimed |
| Stable definition and subobject identities | `crates/orna-core/src/lib.rs`, `catalogue.rs`, `revision.rs` | Core identity, revision, and deterministic catalogue tests | Installed storage/recovery tests are Compose-gated where marked `#[ignore]` |
| SQL AST and domain checker | `crates/orna-compiler/src/relational.rs`, `mutation.rs`, `resolver.rs` | Relational, mutation, and resolver checker tests | Installed execution tests are Compose-gated `#[ignore]` tests |
| Capability and security checker | `crates/orna-core/src/security.rs`, `crates/orna-client/src/capability.rs` | Capability vocabulary, scope, and checked-plan tests; `crates/orna-compiler/src/resolver.rs::tests::accepts_exercised_client_capability_and_rejects_unexercised_declaration` passed with one focused test for matching CLIENT call/capability retention and exact rejection of an unexercised declaration; `crates/orna-server/tests/security_admin.rs::malformed_command_shapes_all_fail_closed_with_exact_usage`; `crates/orna-server/tests/security_admin.rs::valid_canonical_identity_reaches_the_service_account_boundary` is a conditional non-`orna` local boundary check; when the suite runs as `orna`, it returns early instead of asserting the boundary | `standard_database.rs::proves_the_capability_gate_end_to_end` is Compose-gated `#[ignore]`; no local Compose result is claimed |
| Source, revision, and semantic diff (`orna source diff`, without apply) | `crates/orna-core/src/revision.rs`, `catalogue_diff.rs`, `crates/orna-server/src/source_diff.rs` | Identity-keyed diff and revision tests; `crates/orna-server/tests/source_diff.rs::valid_v1_source_reaches_service_account_boundary` is a conditional non-`orna` local boundary check; when the suite runs as `orna`, it returns early instead of asserting the boundary | `crates/orna-server/tests/source_diff_live.rs::proves_installed_source_diff_end_to_end` is Compose-gated `#[ignore]`; pending |

## Server and persistence

| Accepted slice | Implementation evidence | Focused local evidence | Installed or environment-gated proof |
|---|---|---|---|
| Object-relational catalogue and accepted query/mutation storage | `crates/orna-core/src/catalogue.rs`, `crates/orna-postgres/src/kernel/storage.rs` | Catalogue and physical-plan checks | `server_execution.rs` and `server_mutation_execution.rs` installed rows are Compose-gated `#[ignore]` tests |
| Typed `REF<T>` fields, selects, and mutations | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-postgres` | Reference resolution, codec, and planner tests | Authenticated reference execution tests are Compose-gated `#[ignore]` |
| Authentication, session principal, and sealed invocation audit | `crates/orna-core/src/security.rs`, `crates/orna-server/src/local_auth.rs`, `crates/orna-postgres/src/kernel/security.rs` | Security decision shape, principal, and audit codec tests | `standard_database.rs` and `server_execution.rs` live principal/audit tests are Compose-gated `#[ignore]` |
| SERVER executor | `crates/orna-postgres/src/kernel/server_execution.rs`, `server_mutation_execution.rs` | Compiler and kernel preflight checks | Installed query/mutation execution tests are Compose-gated `#[ignore]` |
| Raw typed call and streaming | `crates/orna-server/src/raw_call.rs`, `crates/orna-protocol/src/frame.rs` | `crates/orna-server/tests/raw_call.rs` boundary tests and protocol frame round trips run without Compose | The installed socket/database path remains environment-gated; no local installed proof is claimed |
| Sealed `sys.invoke` | `crates/orna-core/src/system.rs`, `crates/orna-server/src/invoke.rs` | Request, carrier, frame, and authorization unit tests | `standard_database.rs::proves_standard_invocation_dogfooding_through_sealed_sys_invoke` and `proves_installed_orna_invoke_end_to_end_against_postgres` are Compose-gated `#[ignore]` |
| Authenticated USER state service | `crates/orna-core/src/state.rs`, `crates/orna-server/src/user_state.rs`, `crates/orna-postgres/src/kernel/state.rs` | State declaration/parser, plan, lifecycle, conflict, and codec tests; `crates/orna-server/tests/user_state.rs::malformed_state_shapes_all_fail_closed_with_exact_usage`; `crates/orna-server/tests/user_state.rs::valid_state_shapes_reach_the_service_account_boundary` is a conditional non-`orna` local boundary check; when the suite runs as `orna`, it returns early instead of asserting the boundary | The current `just kernel-test` matrix passed the user-state integration paths; standalone package selection, clean-host deployment, and any unrun environment-specific result remain separately gated |
| Headless Inspector carrier, epoch snapshots, and bounded populated projections (ADRs 0080-0081, 0086) | `crates/orna-core/src/inspect.rs`, `crates/orna-server/src/inspect.rs`, `crates/orna-postgres/src/kernel/inspect.rs` (`resource_rows_from_capture`, `ui_nodes_from_events`, `presentation_candidates_from_capture`, `runtime_bindings_from_offer`) | Headless carrier envelope, epoch, redaction, provenance, recursion, and plan tests remain; local helper/epoch targets include `crates/orna-postgres/src/kernel/inspect.rs::persisted_empty_count_is_valid_without_remaining_items`, `allowed_completed_event_supplies_duration_and_completed_phase`, `allowed_without_completed_event_is_executing_without_duration`, `populated_resource_projection_is_active_and_bounded`, `populated_ui_projection_uses_root_function_and_omits_unlabelled_nodes`, and `populated_presentation_projection_uses_final_event_value`; the current `cargo test -p orna-postgres --lib server_execution` run records 90 tests with existing warnings after the selector changes; the focused resource/UI/presentation cases remain present | Bounded resource/UI/presentation/runtime rows are implementation-present; the current `just kernel-test` matrix covers installed populated-Inspector and Inspector-recursion paths. Standalone package selection, clean-host deployment, and full UI-tree/resource-lifecycle/model semantics remain separately gated |
| Recovery path | `crates/orna-postgres/src/kernel/recovery.rs` | Recovery decoder and invariant tests; `cargo test -p orna-postgres --lib recovery` passed with 39 tests; current source includes the Compose-gated checks `crates/orna-postgres/tests/recovery.rs::recovery_rejects_resource_audit_without_its_durable_request_reservation` (resource-request reservation recovery) and `crates/orna-postgres/tests/recovery.rs::recovers_closed_security_audit_history_under_hostile_search_path_and_rejects_tamper` (hostile search-path recovery) | The current `just kernel-test` matrix passed the Compose recovery/integration paths; no standalone invocation result is claimed |
| Standard-library V3-to-V4 upgrade parent guard | `crates/orna-standard/src/lib.rs` | `crates/orna-standard/src/lib.rs::tests::v3_to_v4_upgrade_rejects_non_v3_parents_before_child_work`; the exact focused check `cargo test -p orna-standard --lib v3_to_v4_upgrade_rejects_non_v3_parents_before_child_work -- --nocapture` returned 1 success and rejects the wrong parent before child work | Installed and Compose proof are not claimed |

| Source-apply/source-diff recovery error mapping | `crates/orna-server/src/source_apply.rs`, `crates/orna-server/src/source_diff.rs` | `crates/orna-server/src/source_apply.rs::tests::recovery_database_maps_to_recovery_stage` and `crates/orna-server/src/source_diff.rs::tests::recovery_database_maps_to_recovery_stage_not_attach`; exact checks are `cargo test -p orna-server --lib recovery_database_maps_to_recovery_stage` and `cargo test -p orna-server --lib recovery_database_maps_to_recovery_stage_not_attach`; no current result is claimed | These are local library checks, not PostgreSQL/Compose proof; validation remains unclaimed in this docs-only update |
| Installed one-file source apply and protected `SourceApply` audit | `crates/orna-server/src/source_apply.rs`, `crates/orna-postgres/src/kernel/apply.rs`, `crates/orna-postgres/src/kernel/security.rs` | `crates/orna-server/tests/source_apply.rs` boundary tests `usage_shape_failures_all_fail_closed_with_exact_usage`, `missing_and_non_regular_sources_fail_closed`, `invalid_utf8_source_fails_closed`, and `valid_source_is_read_then_fails_at_the_service_account_boundary` (conditional non-`orna` boundary check; when the suite runs as `orna`, the test returns early rather than asserting the boundary); `crates/orna-postgres/src/kernel/apply.rs` preflight/codec tests; `crates/orna-server/src/source_apply.rs::tests::post_apply_recovery_invariant_fails_closed_as_recovery_mismatch` (`8af410c`); and `crates/orna-postgres/src/kernel/bootstrap.rs::migration_registry_is_a_strict_contiguous_sequence` plus `crates/orna-postgres/src/kernel/bootstrap.rs::source_apply_audit_migration_admits_only_committed_candidates`; `crates/orna-postgres/src/kernel/apply.rs::apply_transaction` calls `establish_trusted_search_path` before locking or recovering the active pair, and `crates/orna-postgres/src/kernel/physical.rs::establish_trusted_search_path` issues `SET LOCAL search_path = pg_catalog` | `crates/orna-postgres/tests/apply.rs::applies_source_apply_and_records_one_protected_audit_event` is Compose-gated `#[ignore]`; rollback and tamper cases are also pending; no local product proof is claimed |
| Retained revision-pair listing kernel contract | `crates/orna-postgres/src/kernel/recovery.rs` | `revision_pair_listing_rejects_orphan_parent`, `revision_pair_listing_rejects_duplicate_source_identity`, `revision_pair_listing_rejects_duplicate_catalogue_identity`, `revision_pair_listing_rejects_cycles`, `revision_pair_listing_rejects_mismatched_parent_source`, `revision_pair_listing_requires_exactly_one_active_pair`, and `revision_pair_listing_rejects_multiple_active_pairs` | `crates/orna-postgres/tests/apply.rs::lists_revision_pairs_with_parent_links_and_active_candidate` is Compose-gated `#[ignore]`; there is no public `orna revisions list` proof |
| Compose-gated source-apply audit, rollback, tamper, and retained-listing integration proof | `crates/orna-postgres/tests/apply.rs` | The current `just kernel-test` matrix passed the Compose apply/integration paths; implementation and focused validators are tracked in the two rows above | Standalone package selection, clean-host deployment, and any unrun environment-specific result remain separately gated. `crates/orna-postgres/tests/apply.rs::applies_source_apply_and_records_one_protected_audit_event` and the rollback/tamper/retained-listing cases remain Compose-service evidence |

## Standard library

The rows below expand the accepted standard-library checklist into the concrete
source, catalogue, artifact, runtime, and server-test seams. Focused test names
are evidence targets, not claims that this documentation-only update reran
them. `std.launch`, `std.protocol`, general Rows/object-value semantics, and
deferred presenter/model semantics are intentionally outside this section's claims.

| Accepted slice | Implementation evidence | Focused local evidence | Installed or environment-gated proof |
|---|---|---|---|
| `std.terminal` transient `Document` value and terminal sink | `stdlib/std/output.orna`; `crates/orna-standard/src/lib.rs::standard_library_v3_manifest`; `crates/orna-compiler/src/resolver.rs::check_standard_library_source_v3` and `reconcile_standard_output_unit`; `crates/orna-runtime-tty/src/lib.rs::render_document` | `crates/orna-standard/src/lib.rs::tests::manifest_v3_exposes_the_reserved_output_standard_facts` and `crates/orna-standard/src/lib.rs::tests::v3_registered_opaque_codecs_construct_the_output_payloads`; `crates/orna-compiler/src/resolver.rs::tests::reconciles_the_exact_v3_standard_output_bundle`; `crates/orna-runtime-tty/src/lib.rs::tests::renders_document_verbatim`, `crates/orna-runtime-tty/src/lib.rs::tests::rejects_document_with_wrong_magic`, and `crates/orna-runtime-tty/src/lib.rs::tests::rejects_document_with_invalid_utf8` (focused targets present; not rerun here) | The terminal renderer is local and database-independent. Installed `orna` output/invoke coverage is in `crates/orna-server/tests/standard_database.rs::proves_output_through_orna_invoke_against_postgres`, a Compose-gated `#[ignore]` proof; no installed result is claimed. The accepted Qt v1 runtime is tracked separately; installed/native proof and second toolkit/browser families remain deferred. |
| `std.json` V5 `Value` and `std.json.encode` | `stdlib/std/json.orna`; `crates/orna-standard/src/lib.rs::standard_library_v5_manifest`; `crates/orna-compiler/src/resolver.rs::reconcile_standard_json_unit` and `check_standard_json_encode`; `crates/orna-artifact/src/server_json_encode.rs::JsonEncodePlan`; `crates/orna-postgres/src/kernel/server_execution.rs::execute_standard_json_encode` | `crates/orna-standard/src/lib.rs::tests::retains_and_verifies_v5_json_standard_snapshot` and `crates/orna-standard/src/lib.rs::tests::v5_json_codec_rejects_non_canonical_body_bytes`; `crates/orna-compiler/src/resolver.rs::tests::checks_the_exact_json_encode_presenter_declaration`; `crates/orna-server/tests/json_v5_reopen.rs::v5_json_encode_survives_reopen_with_exact_retained_identity` checks retained identity and canonical payload framing (focused targets present; not rerun here) | `crates/orna-server/tests/standard_database.rs::proves_v5_json_value_and_encode_through_installed_sealed_invoke` and `crates/orna-server/tests/json_v5_reopen.rs::v5_json_encode_survives_reopen_with_exact_retained_identity` are Compose-gated `#[ignore]` tests; pending and no local Compose result is claimed. JSON is an accepted presenter/value slice, not a general value or gateway contract. |
| `std.present` fixed presenter registry and output resolution (accepted JSON/table/CSV presenter metadata only) | `crates/orna-core/src/presenter.rs::PresenterRegistry::new`, `PresenterRegistry::resolve_requirement`, `PresenterRegistry::resolve_alias`, and `PresenterRegistry::resolve_media_type`; `crates/orna-compiler/src/resolver.rs::check_standard_json_encode`, `check_standard_terminal_present_table`, and `reconcile_standard_output_unit`; `crates/orna-postgres/src/kernel/server_execution.rs::execute_standard_json_encode` and `execute_standard_terminal_present_table` | `crates/orna-core/src/presenter.rs::tests::requirement_resolution_prefers_alias_over_media_type`; `crates/orna-compiler/src/resolver.rs::tests::checks_the_exact_json_encode_presenter_declaration` and `crates/orna-compiler/src/resolver.rs::tests::checks_the_exact_terminal_present_table_presenter_declaration`; `crates/orna-server/tests/standard_database.rs::proves_output_through_orna_invoke_against_postgres` is the focused end-to-end target (not rerun here) | The end-to-end presenter/output test is Compose-gated `#[ignore]` and remains proof-pending. This row does not claim general `Rows` semantics or deferred presenter/model planning/execution. |
| `std.ui.window@1` V7 retained CLIENT entry point (ADR 0083) | `stdlib/std/window.orna`; `crates/orna-standard/src/lib.rs` V7 manifest and retained snapshot; `crates/orna-compiler/src/resolver.rs::check_standard_ui_window`; `crates/orna-client/src/runtime_adapter.rs::QtRuntimeExecutor::execute_window` | Retained V7 source/catalogue/executable reconciliation, canonical `ORNA-UI/1` validation, and the window-only adapter boundary are implementation evidence; `just studio-qt-smoke target/runtime-qt/liborna-runtime-qt.so` passed with displayed canonical UI and shutdown | Installed Qt package selection, installed UI, and clean-host proof remain pending; the explicit local smoke does not claim Compose/Docker or installed-runtime evidence |
| `std.data.Rows` V8 and retained table presenter (ADR 0087) | `stdlib/std/data.orna`; `crates/orna-standard/src/lib.rs` V8 manifest, retained snapshot, Rows codec registration, and `prepare_standard_upgrade_v7_to_v8`; `crates/orna-compiler/src/resolver.rs` V8 source reconciliation; `crates/orna-core/src/value.rs` Rows value boundary; `crates/orna-protocol/src/lib.rs` Rows framing; `crates/orna-postgres/src/kernel/server_execution.rs` retained table/CSV routing and shape-preserving result retention | `crates/orna-standard/src/lib.rs::tests::v8_rows_snapshot_retains_canonical_source_and_digests`; `crates/orna-postgres/src/kernel/server_execution.rs::tests::sealed_rows_value_preserves_complete_shape_for_table_and_csv`; `crates/orna-postgres/src/kernel/server_execution.rs::tests::sealed_rows_zero_row_result_stays_one_value_batch_item` | Bounded materialised Rows and shape-preserving table/CSV presentation are accepted; V7-to-V8 install/reopen/persistence and end-to-end presentation remain Compose-gated and unrun, with no Compose result claimed. Rows resources, virtual models, lossless JSON, and general object-value semantics remain deferred |
| V9 structural UI constructors for source-authored CLIENT work (ADR 0088) | `stdlib/std/ui_constructors.orna`; `crates/orna-standard/src/lib.rs` V9 manifest, retained snapshot, and `prepare_standard_upgrade_v8_to_v9`; `crates/orna-compiler/src/resolver.rs::check_standard_library_source_v9` and `check_standard_ui_constructor`; `crates/orna-client/src/lib.rs` V9 constructor intrinsic | `crates/orna-standard/src/lib.rs::tests::v9_ui_constructors_snapshot_retains_source_digests_and_codecs`; `crates/orna-client/src/lib.rs::standard_ui_text_constructor_builds_canonical_value_without_executor`, `standard_ui_constructors_build_all_closed_mappings_and_singleton_nesting`, `standard_ui_constructor_dispatches_without_an_executor`, `standard_ui_constructor_rejects_wrong_order_and_runtime_kinds`, and `standard_ui_constructor_rejects_malformed_content_frame`; `crates/orna-server/src/source_check.rs::tests::checks_static_ui_source_fixture_against_verified_v9_standard`; current V9 package-suite counts are recorded in the evidence section above | The static source/compiler/value slice is accepted; explicit Qt/window smoke is recorded above, while installed package selection and V8-to-V9 persistence/recovery remain unrun; actions/events, models, dynamic metadata, input/focus, and full Studio remain deferred; no GUI package-install, Compose, or installed UI result is claimed |
| `std.data` typed scalar/stream resource constructors and transport (no general model/`Rows` claim) | `crates/orna-standard/src/lib.rs::retained_standard_library_v2_snapshot` and `verify_standard_library_v2_snapshot` (accepted standard authority supplied to resource checks); `crates/orna-compiler/src/resolver.rs::resource_constructor_kind`, `client_resource_target`, and `client_local_resource_family`; `crates/orna-artifact/src/client_plan.rs::ResourceKind`, `ResourceOperationNode`, and `RESOURCE_FORMAT_VERSION`; `crates/orna-protocol/src/frame.rs::ResourceKind`, `encode_resource_request`, and `decode_resource_request` | `crates/orna-compiler/src/resolver.rs::tests::accepts_procedural_scalar_resource_local_await`, `crates/orna-compiler/src/resolver.rs::tests::accepts_scalar_resource_assignment_await_with_exact_spans_and_call_provenance`, and `crates/orna-compiler/src/resolver.rs::tests::discovers_stream_resource_target_with_resolved_element_type`; `crates/orna-artifact/src/client_plan.rs::tests::resource_plan_round_trips_kind_revision_call_site_arguments_and_await`; `crates/orna-server/tests/standard_database.rs::checks_accepted_scalar_resource_fixture_offline` and `crates/orna-server/tests/standard_database.rs::checks_accepted_stream_resource_fixture_offline` (focused targets present; not rerun here) | Installed scalar/procedural/stream evaluator and socket tests in `crates/orna-server/tests/standard_database.rs` are Compose-gated `#[ignore]` (including `proves_scalar_client_resource_pending_continues_through_installed_evaluator`, `proves_procedural_client_resource_through_installed_evaluator`, and `proves_installed_no_broker_resource_returns_typed_result_and_terminal`); no installed result is claimed. Broader model ecosystem and general `Rows` semantics remain deferred. |
| `std.action` opaque action values and accepted `std.action.call` constructor | `stdlib/std/action.orna`; `crates/orna-standard/src/lib.rs::standard_library_v6_manifest`; `crates/orna-compiler/src/resolver.rs::check_standard_library_source_v6`, `check_action_constructor`, and `client_action_targets`; `crates/orna-artifact/src/client_plan.rs::ACTION_FORMAT_VERSION` and action operation encoding; `crates/orna-client/src/lib.rs::trigger_client_action` | `crates/orna-standard/src/lib.rs::tests::retains_and_verifies_v6_action_standard_snapshot` and `crates/orna-standard/src/lib.rs::tests::v6_action_codec_rejects_malformed_descriptor_structure`; `crates/orna-compiler/src/resolver.rs::tests::accepts_client_action_call_with_canonical_server_target`, `crates/orna-compiler/src/resolver.rs::tests::accepts_transient_standard_opaque_action_target_result`, and `crates/orna-compiler/src/resolver.rs::tests::excludes_stream_and_one_column_rows_action_targets`; `crates/orna-server/tests/standard_database.rs::checks_accepted_action_fixture_offline`; `crates/orna-client/src/lib.rs::action_target_result_type_rejects_stream_targets` and `crates/orna-client/src/lib.rs::action_trigger_after_terminal_completion_allocates_fresh_request_identity` (focused targets present; not rerun here) | `crates/orna-server/tests/standard_database.rs::proves_server_action_resource_trigger_through_authenticated_executor` and `proves_server_action_denial_stays_inside_authenticated_resource_trigger` are Compose-gated `#[ignore]` proofs; pending. The accepted slice is `std.action.call`; stream/one-column action targets and broader action combinators are not claimed. |
| `std.security` administration and sealed security identities | `crates/orna-core/src/security.rs`; `crates/orna-core/src/system.rs::security_entries_expose_the_sealed_function_ids_and_names_in_order` and `security_entries_expose_the_closed_signature_shapes`; `crates/orna-server/src/security_admin.rs::InstalledSecurityAdminOperation` and `run_installed_security_grant`; `crates/orna-postgres/src/kernel/security_admin.rs` | `crates/orna-server/tests/security_admin.rs::malformed_command_shapes_all_fail_closed_with_exact_usage`; `crates/orna-server/tests/security_admin.rs::valid_canonical_identity_reaches_the_service_account_boundary`; `crates/orna-core/src/system.rs::security_entries_expose_the_closed_signature_shapes` (focused local targets present; the conditional service-account test may self-skip under `orna`; not rerun here) | `crates/orna-server/tests/security_admin_live.rs::proves_installed_security_admin_end_to_end` is Compose-gated `#[ignore]`; live grant/admin success, database row effects, and hostile endpoint-environment rejection remain unclaimed. |

## Client, resources, actions, and Inspector

| Accepted slice | Implementation evidence | Focused local evidence | Installed or environment-gated proof |
|---|---|---|---|
| `orna` CLI and offline source checking | `crates/orna-server/src/main.rs`, source-check command | `source_check.rs` and command-boundary tests without PostgreSQL, network, configuration, or writes | No database-backed installed proof is required for the offline check; other installed commands remain separately gated |
| Automatic runtime selection and TTY output presenters | `crates/orna-server/src/invoke.rs`, `crates/orna-runtime-tty`, `crates/orna-core/src/presenter.rs` | `crates/orna-server/src/invoke.rs::selection_policy_defaults_to_tty_and_rejects_unknown_families`, `the_client_offer_names_the_tty_runtime`, and `tty_default_selection_maps_document_and_byte_stream`; runtime presenter and output codec tests | `crates/orna-server/tests/standard_database.rs::proves_installed_orna_invoke_end_to_end_against_postgres` and `proves_output_through_orna_invoke_against_postgres` are Compose-gated `#[ignore]`; no local Compose result is claimed. Qt v1 is accepted in the separate runtime row; second toolkit/browser and installed/native proof remain deferred |
| Accepted Qt v1 runtime ABI/provider and installed package boundary (ADRs 0082, 0085) | `runtimes/qt/CMakeLists.txt`, `runtimes/qt/src/runtime.cpp`, `runtimes/qt/tests/runtime_test.cpp`, `crates/orna-client/src/runtime_loader.rs`, `crates/orna-client/src/runtime_adapter.rs`, `crates/orna-server/src/invoke.rs`, `packaging/debian/control`, and `packaging/debian/rules`; canonical ABI input remains external `../spec/spec/orna_runtime_abi_v1.h` | `just runtime-qt-test` passed (CMake build and ctest 1/1); `just runtime-qt-rust-smoke target/runtime-qt/liborna-runtime-qt.so` passed with canonical state 1191 bytes; `just studio-qt-smoke target/runtime-qt/liborna-runtime-qt.so` passed with displayed canonical UI and shutdown; `just runtime-abi-header-check && just runtime-abi-parity` passed | The Linux x86_64 Qt v1 ABI/provider, pathless offer, fixed package path, and loader boundary are accepted implementation slices. Installed runtime package, clean-host, and installed UI proof remain pending; no Compose/Docker or installed-runtime result is claimed |
| CLIENT expression bodies and external `RUNTIME CONTRACT` clauses (ADR 0068) | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-artifact` | `parses_client_expression_bodies_and_external_contracts_with_exact_spans`; expression-plan version-three round trips/rejection tests; resolver contract tests; `6e40b72`: `crates/orna-server/tests/standard_database.rs::checks_accepted_expression_client_fixture_offline` | `crates/orna-server/tests/standard_database.rs::proves_expression_client_functions_through_installed_invoke` is Compose-gated `#[ignore]`; no local Compose result is claimed; pending |
| CLIENT STATE declarations and version-four plans (ADRs 0069-0070) | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-artifact`, `crates/orna-client` | State parser tests, procedural plan tests, and client state lifecycle unit tests | Authenticated USER-state proofs in `user_state_live.rs` are Compose-gated `#[ignore]`; pending |
| Executor-independent CLIENT resource lifecycle (ADRs 0071 and 0074) | `crates/orna-client`, `crates/orna-artifact` | `client_resource_lifecycle_rejects_stale_and_invalid_results`, resource request context checks, and lifecycle unit tests; `crates/orna-client/src/lib.rs::resource_invalidation_cancels_owned_request_and_rejects_late_completion`; `resource_invalidation_preflights_generation_before_releasing_request`; `resource_invalidation_retains_owned_request_when_abandon_fails`; `replacing_resource_key_retains_nested_request_when_abandon_fails`; `nested_action_pending_cancel_retains_replacements_and_exact_child`; `nested_action_malformed_child_pending_cancel_retains_exact_identity`; `nested_executor_rejects_mismatched_completion_identity`; commit `538769d` records the current ownership and exact-identity hardening | Installed evaluator/resource proofs are Compose-gated `#[ignore]`; no local Compose result is claimed |
| CLIENT resource revision witness and stale replacement ownership | `crates/orna-client/src/lib.rs` | `ClientResourceValidationContext` captures the pinned `RevisionPair`, target/kind/type admission, and value-validation facts without cloning `ActiveDatabaseRevision`; `stale_replacement_uses_pinned_validation_after_revision_changes`; historical reports (not current validation) recorded 278 and later 283 client-library tests; current `cargo test -p orna-client` after V9 work passed with 326 tests and existing warnings | Installed evaluator/resource proofs remain Compose-gated `#[ignore]`; no local Compose result is claimed |

| CLIENT-to-SERVER resource language with typed scalar/stream constructors and `AWAIT` (ADR 0077) | `crates/orna-syntax`, `crates/orna-compiler`, `crates/orna-artifact` | `parses_client_await_expression_losslessly_with_complete_span`, `parses_client_local_resource_binding_and_await_return`, `resource_plan_round_trips_kind_revision_call_site_arguments_and_await`, `procedural_plan_round_trips_scalar_resource_await_in_assignment`, procedural resource checks, and resolver target/type tests; `crates/orna-server/tests/standard_database.rs::checks_accepted_scalar_resource_fixture_offline` and `crates/orna-server/tests/standard_database.rs::checks_accepted_stream_resource_fixture_offline`; `c58869d`: `crates/orna-compiler/src/resolver.rs::sorts_resource_and_action_arguments_by_checked_parameter_id` proves resource arguments use canonical `CheckedParameterId` order despite reversed source order | `standard_database.rs::proves_scalar_client_resource_pending_continues_through_installed_evaluator`, `proves_procedural_client_resource_through_installed_evaluator`, and `proves_installed_no_broker_resource_returns_typed_result_and_terminal` are Compose-gated `#[ignore]`; the focused artifact proof is current; no local installed resource result is claimed |

| Authenticated `ORNA-RESOURCE/1` transport, credits, cancellation, ordering, and scheduling (ADR 0078) | `crates/orna-protocol`, `crates/orna-client`, `crates/orna-server`, `crates/orna-postgres` | Resource frame/codec goldens and malformed-frame tests; `crates/orna-protocol/src/frame.rs::resource_frames_have_exact_golden_bytes` and `crates/orna-protocol/src/frame.rs::resource_result_revision_mismatch_precedes_value_credit_and_terminal_mutation` prove exact ORNA-RESOURCE result target-revision encoding and fail-closed mismatch rejection; current sealed-event lifecycle evidence is recorded above; `crates/orna-client/src/lib.rs::client_stream_rejects_oversized_batches_and_totals_before_queueing`; `crates/orna-client/src/lib.rs::client_stream_queue_overflow_preserves_existing_batches`; `crates/orna-client/src/lib.rs::client_stream_queue_dequeue_releases_capacity`; `crates/orna-protocol/src/frame.rs::resource_values_reject_declared_payload_bound_before_encoding_values`; `crates/orna-protocol/src/frame.rs::resource_connection_rejects_unknown_lower_stream_without_tombstone` (`b1ba1ba`); `crates/orna-protocol/src/frame.rs::resource_connection_bounds_request_id_history_without_eviction`; `crates/orna-protocol/src/frame.rs::resource_connection_rejects_duplicate_request_id_when_history_is_exhausted`; `crates/orna-protocol/src/frame.rs::resource_connection_cancellation_and_shutdown_drop_late_frames`; `crates/orna-protocol/src/frame.rs::resource_terminal_tombstones_retain_oldest_cancelled_stream`; `crates/orna-protocol/src/frame.rs::resource_connection_tracks_acceptance_credit_sequence_and_terminal_late_frames`; `crates/orna-protocol/src/frame.rs::resource_connection_reports_item_and_byte_credit_exhaustion_independently`; `crates/orna-protocol/src/frame.rs::resource_connection_apply_rejects_unbounded_direct_batch_metadata`; `crates/orna-protocol/src/frame.rs::resource_values_reject_truncated_value_length_without_mutating_connection` passed its focused check and preserves decoded connection state and unrelated stream credit; these checks do not establish a broader malformed resource-frame isolation claim; local scheduler coverage includes `crates/orna-server/src/raw_socket.rs::scheduled_shutdown_task_returns_without_awaiting_cleanup`, `queued_resource_permit_waiter_completes_when_cancelled`, `constructed_resource_stream_queued_completion_wins_over_credit_blocked_cancellation`, `constructed_resource_cancellation_wins_before_dispatch_completion`, `queued_resource_completion_wins_over_cancellation`, `committing_resource_cancellation_does_not_terminalise_stream`, and `server_shutdown_cancels_active_resource_without_emitting_a_terminal_frame`; `crates/orna-server/src/raw_socket.rs::queued_internal_failure_is_not_replaced_by_cancel` proves raw cancellation does not replace an internal failure (focused command `cargo test -p orna-server --lib queued_`, 5 passed); broader operational raw transport proof remains limited to local focused coverage and Compose-gated installed tests. Shared-broker coverage includes `crates/orna-server/src/invoke.rs::shared_broker_reader_retains_partial_frames_across_polling`, `broker_cleanup_completion_signal_does_not_wait_for_full_queue`, `broker_stream_completion_queue_is_finite`, `shared_broker_drops_known_terminal_frames_and_rejects_unknown_streams`, `shared_broker_resource_expectations_require_exact_request_identity`, `shared_broker_terminal_tombstones_are_bounded`, and `shared_broker_drops_evicted_terminal_frames_by_high_water_mark`; detached executor coverage includes `installed_executor_abandon_closes_raw_controls_after_late_values`, `detached_raw_poll_publishes_terminal_after_timeout_marker`, `detached_broker_poll_publishes_terminal_after_timeout_marker`, `detached_broker_abandon_suppresses_delayed_terminal`, `installed_executor_detached_raw_abandon_requires_exact_identity`, `installed_executor_detached_broker_abandon_requires_exact_identity`, `installed_executor_broker_abandon_retains_pending_on_closed_channel`, `installed_executor_detached_raw_cancel_requires_exact_identity`, and `installed_executor_detached_broker_cancel_requires_exact_identity` | Installed stream target proof `crates/orna-server/tests/standard_database.rs::authenticated_stream_resource_dispatches_allowed_and_denied_with_redacted_audit` is Compose-gated `#[ignore]`; scalar/stream, denial, cancellation, and terminal-order proofs in `standard_database.rs` are Compose-gated `#[ignore]`; no local Compose result is claimed; pending. Bead `ornadb-el1.2.1.43` remains in progress for terminal provenance; Bead `ornadb-el1.2.1.44` is closed after focused direct/shared precedence alignment. Authority and live-producer evidence remain pending, and no wire or provenance completion claim is made |
| Current accepted security and audit transaction hardening (2026-08-25) | `crates/orna-postgres/src/kernel/security.rs`, `crates/orna-postgres/src/kernel/inspect.rs`, `crates/orna-postgres/tests/recovery.rs` | `resource_target_security_is_supported` guard and focused denial/audit unit checks; trusted `SET LOCAL search_path = pg_catalog` setup in fresh audit transactions; `cargo check -p orna-postgres` passed with existing warnings | `authenticated_resource_denies_recovered_security_definer_before_execution` passed against the local Compose PostgreSQL service; the read-only fixture limitation and hostile-search-path recovery coverage remain tracked separately. Bead `ornadb-el1.2.1.28.1` remains open as a recorded contract/fixture blocker; no mutation side-effect proof is claimed |


| ORNA-RESOURCE request lineage identity validation | `crates/orna-protocol/src/frame.rs`, `crates/orna-client/src/lib.rs`, `crates/orna-postgres/src/kernel/security.rs` | `resource_request_rejects_zero_parent_invocation_id_at_encode_decode_and_open` and `resource_request_rejects_zero_call_site_id_at_encode_decode_and_open` prove fail-closed encode/decode/open rejection with unchanged connection state; `tests::resource_request_rejects_zero_lineage_before_loading` proves direct CLIENT rejection leaves the resource idle with generation 0 and no request identity; `resource_lineage_validation_rejects_zero_parent_before_other_request_validation`, `resource_lineage_validation_rejects_zero_call_site_before_other_request_validation`, and `resource_lineage_validation_accepts_non_zero_identities_without_mutation` prove the Postgres validator's precedence and no-mutation contract; focused CLIENT and Postgres commands pass | Live producer/audit database proof remains environment-gated; no Compose result is claimed |
| Current accepted CLIENT USER write-context admission (ADR 0070/0077, 2026-08-25) | `crates/orna-client/src/lib.rs::ClientStateStore::set_user_state`, `ClientUserStateError::ContextMismatch` | `client_user_state_set_rejects_context_mismatch_before_lookup_or_mutation` proves root/profile/instance rejection before type lookup or map mutation, unchanged user/pending state, and matching revision-preserving update; the older 278-pass report and later 283-test source record are historical; current `cargo test -p orna-client` after V9 work passed with 326 tests and existing warnings | Authenticated USER-state live proofs remain Compose-gated `#[ignore]`; no live database result is claimed |
| Current bounded V8 `std.data.Rows` and shape-preserving table/CSV presentation (ADR 0087, 2026-08-27) | `stdlib/std/data.orna`; `crates/orna-standard/src/lib.rs`; `crates/orna-protocol/src/lib.rs`; `crates/orna-core/src/presenter.rs`; `crates/orna-postgres/src/kernel/server_execution.rs::sealed_result_rows`, `execute_standard_terminal_table`, and `execute_standard_csv_encode` | `crates/orna-postgres/src/kernel/server_execution.rs::sealed_rows_value_preserves_complete_shape_for_table_and_csv`; `sealed_rows_zero_row_result_stays_one_value_batch_item`; selector and presenter-resolution symbols remain in `resolve_sealed_presenter_type_name` and `present_sealed_standard_output`; the 355-test `cargo test -p orna-postgres --lib` report predates selector-only changes and is historical for those changes | The bounded Rows value and retained table/CSV shape are implementation-present; V7-to-V8 install/reopen, persistence recovery, and installed presenter invocation remain Compose-gated and unrun, with no Compose result claimed. No general Rows resource, virtual model, lossless JSON, or scalar-contract expansion is claimed |
| Current V9 standard/compiler/selector evidence (ADRs 0087-0088, 2026-08-27) | `crates/orna-standard/src/lib.rs::prepare_standard_upgrade_v7_to_v8` and `prepare_standard_upgrade_v8_to_v9`; `crates/orna-compiler/src/resolver.rs`; `crates/orna-client/src/lib.rs`; `crates/orna-postgres/src/kernel/server_execution.rs::resolve_sealed_presenter_type_name` and `present_sealed_standard_output` | `cargo test -p orna-standard --lib` passed with 88 tests after V9 work; `cargo test -p orna-compiler --lib` passed with 471 tests; `cargo test -p orna-client` passed with 326 tests with existing warnings; upgrade guards include `prepares_the_v7_to_v8_standard_upgrade_from_an_empty_v7_active_revision`, `v7_to_v8_upgrade_rejects_non_v7_parents_before_child_work`, `prepares_the_v8_to_v9_standard_upgrade_from_an_empty_v8_active_revision`, and `v8_to_v9_upgrade_rejects_non_v8_parents_before_child_work`; selector-focused targets are listed above without a post-change suite-count claim | V7-to-V8-to-V9 persistence and recovery remain Compose-gated; no Compose run or installed V9/Rows proof is claimed. V9 construction is bounded to the accepted seven UI constructors; model, action/event, gateway, and full Studio surfaces remain deferred |
| Current post-cancellation raw and resource transport guards (ADRs 0054/0078, 2026-08-25) | `crates/orna-protocol/src/frame.rs`, `crates/orna-server/src/invoke.rs` | Raw cancellation guard rejects stale non-Internal failures before state mutation; late resource Accepted/Values frames are drained before local shape checks while committed terminals remain; focused frame, broker, socket-transport, and `cargo check -p orna-protocol`/`cargo check -p orna-server` checks passed | Live resource producer and installed transport proofs remain Compose-gated; no environment result is claimed |

| Current accepted-boundary hardening (2026-08-25) | `crates/orna-client/src/lib.rs::ClientStateStore::load_user_state`; `crates/orna-compiler/src/prepare.rs::CandidateBuilder::client_named_type_id`; `crates/orna-postgres/src/kernel/security.rs::validate_resource_lineage` | `client_user_state_load_rejects_wrong_instance_key_atomically`; `named_standard_resource_result_uses_catalogue_value_identity`; `resource_lineage_validation_rejects_zero_request_id_before_other_lineage_checks`; focused client/compiler/Postgres checks passed | Live PostgreSQL producer/audit persistence and Compose installed evaluator proofs remain environment-gated |
| Current accepted resource descriptor, stream-element, and named-record boundaries (2026-08-25) | `crates/orna-compiler/src/resolver.rs`; `crates/orna-protocol/src/frame.rs`; `crates/orna-client/src/lib.rs` | `rejects_client_resource_table_descriptor_with_deferred_row_diagnostic`; `rejects_client_stream_resource_record_descriptor_with_deferred_row_diagnostic`; `discovers_stream_resource_target_with_resolved_enum_element_type`; `rejects_opaque_stream_resource_elements_without_a_checked_bundle`; `discovers_stream_resource_target_with_resolved_record_element_type`; `resource_values_record_round_trip_preserves_canonical_orv3_identity`; `client_record_stream_batches_append_and_take_nominal_type_ids` — focused compiler, protocol, and client proofs pass, covering parser-accepted deferred descriptors, exact rejection diagnostics, durable enum and record admission, canonical ORV3 identity, and nominal `TypeId` validation | No installed or runtime proof is claimed; deferred ROWS transport, opaque values, and row-shaped resource transport remain outside the accepted boundary |

| CLIENT action values and `std.action.call` (ADR 0079) | `stdlib/std/action.orna`, `crates/orna-standard`, `crates/orna-compiler`, `crates/orna-artifact`, `crates/orna-client` | `crates/orna-server/tests/standard_database.rs::checks_accepted_action_fixture_offline`; `crates/orna-client/src/lib.rs::action_trigger_rejects_repeated_pending_server_request_without_mutating_generation`; `crates/orna-client/src/lib.rs::action_trigger_after_terminal_completion_allocates_fresh_request_identity`; `crates/orna-client/src/lib.rs::action_target_result_type_rejects_stream_targets` and `crates/orna-compiler/src/resolver.rs::tests::excludes_stream_and_one_column_rows_action_targets` retain the deferred stream-action target boundary; `crates/orna-standard/src/lib.rs::tests::retains_and_verifies_v6_action_standard_snapshot`, `crates/orna-standard/src/lib.rs::tests::v6_manifest_appends_action_without_changing_v5_catalogue_content`, and `crates/orna-standard/src/lib.rs::tests::v5_and_v6_retain_the_locked_sources_and_catalogue_identities`; `crates/orna-standard/src/lib.rs::tests::retains_and_verifies_v5_json_standard_snapshot`, `crates/orna-standard/src/lib.rs::tests::v5_and_v6_digest_goldens_cover_retained_units_and_catalogues`, and `crates/orna-standard/src/lib.rs::tests::v5_and_v6_opaque_codecs_match_the_append_only_registration_surface` preserve accepted V5 JSON parity; `accepts_client_action_call_with_canonical_server_target`; action-plan round trips and malformed/ordering rejection tests; action lifecycle tests; `crates/orna-client/src/lib.rs::nested_action_pending_cancel_retains_replacements_and_exact_child`; `nested_action_malformed_child_pending_cancel_retains_exact_identity`; `nested_executor_rejects_mismatched_completion_identity`; `action_uncertain_cancel_retains_loading_request`; and `action_current_generation_mismatched_request_is_stale_but_same_request_malformed_completion_cancels`; `c58869d`: `crates/orna-compiler/src/resolver.rs::sorts_resource_and_action_arguments_by_checked_parameter_id` proves action arguments use canonical `CheckedParameterId` order despite reversed source order; `crates/orna-core/src/value.rs` supplies structural V6 opaque-action validation, and `crates/orna-standard/src/lib.rs::tests::v6_action_codec_rejects_malformed_descriptor_structure` records the focused malformed-record regression; work commit `c63f327` also records `crates/orna-compiler/src/prepare.rs::tests::accepted_client_action_preparation_preserves_durable_operation_identity_and_arguments`; the exact focused check returned 1 success and preserves durable operation identity and normalized arguments; the old work commit remains provenance only | `standard_database.rs::proves_server_action_resource_trigger_through_authenticated_executor` and `proves_server_action_denial_stays_inside_authenticated_resource_trigger` are Compose-gated `#[ignore]`; no local Compose result is claimed; pending |

| Test-only headless runtime conformance (ADR 0076) | `crates/orna-client` test fixture | Focused runtime-conformance package tests cover descriptor, ownership, supported-operation batches, typed `OrnaSurfaceClosedEventV1`/`OrnaDiagnosticEventV1` payloads, cancellation, and terminal shutdown in the in-process fixture without PostgreSQL, shared-library loading, or a native runtime; the fixture rejects header-named `SET_FOCUS`/`SET_ACCESSIBILITY` kinds as unsupported because `OrnaUiOperationV1` has no payload arms for them; `ba20fda`: `runtime_conformance::destroying_a_surface_retires_all_owned_handles_and_suppresses_stale_work` proves surface-owned node/action/request retirement, one cancellation, and stale-work suppression | The headless implementation is present; the accepted production Qt v1 ABI/provider is tracked separately, and no installed package, clean-host, or portable production-runtime proof is claimed for this fixture |
| Test-only C/Rust headless runtime ABI parity and accepted Qt ABI checks (ADRs 0076, 0082) | `crates/orna-client/tests/runtime_abi_parity.c`, `justfile`, and canonical external `../spec/spec/orna_runtime_abi_v1.h` | `just runtime-abi-header-check && just runtime-abi-parity` passed; the parity gate covers Linux x86_64 C11 size, alignment, offset, enum, constant, union, callback, and runtime-table assertions against the canonical header and Rust mirror values | These local ABI checks and the separate `just runtime-qt-test`, package build, and Studio smoke cover the accepted Qt v1 provider/package implementation. Installed fixed-path package selection, clean-host deployment, portable ABI conformance, and installed/native UI remain environment-gated; no unrun result is claimed |
| Headless ordinary CLIENT Inspector v1 carrier/epoch and generic `std.inspect.render@1` (ADRs 0080-0081) | `crates/orna-client`, `crates/orna-compiler`, `crates/orna-artifact`, `crates/orna-standard` | Local CLI parser/dispatch and closed exit-code unit tests: `crates/orna-server/src/main.rs::parse_inspect_command`, `crates/orna-server/src/main.rs::Command::Inspect`, `crates/orna-server/src/main.rs::accepts_an_exact_inspect_command`, `crates/orna-server/src/main.rs::accepts_inspect_projection_trace_and_classifiers`, `crates/orna-server/src/main.rs::accepts_every_inspect_projection_name`, `crates/orna-server/src/main.rs::rejects_malformed_inspect_shapes`, and `crates/orna-server/src/main.rs::inspect_error_exit_codes_follow_the_closed_table`; this local CLI evidence is distinct from the carrier/epoch evidence: Inspector carrier/epoch/redaction/lifecycle tests, `inspect_plan_round_trips_all_projections_and_uses_version_nine`, generic render contract acceptance, and fail-closed resolver tests. ADR 0086 bounded resource/UI/presentation/runtime population is recorded in the preceding Inspector row; full UI-tree/model/stream semantics remain deferred | The headless and bounded populated-row implementations are present; the current `just kernel-test` Compose matrix covers installed evaluator, Inspector-recursion, and populated-projection paths. No standalone invocation result is claimed, and clean-host/native-host proof remains separately gated. This does not claim full UI tree, graphical models, or gateways. Bead `ornadb-el1.2.1.33` is closed by ADR 0080: ORNA-INSPECT/1 treats its u64 `epoch_id` as complete v1 wire authority; full-width epoch identity requires a future carrier version and ADR, so no envelope widening is claimed |
| Sealed system identity calls and ORV6 SET (ADRs 0072-0074) | `crates/orna-core`, `crates/orna-protocol`, `crates/orna-standard` | Identity and canonical SET frame golden tests | Installed system identity proofs in `standard_database.rs` are Compose-gated `#[ignore]` |
| V5 JSON value and presenter recovery (ADR 0075) | `crates/orna-core`, `crates/orna-standard`, `crates/orna-artifact`, `crates/orna-server` | V5 source/catalogue/digest goldens; `v5_json_origins_match_declaration_identities_and_exact_source_slices`; ORV5 canonical codec tests; malformed-declaration and plan-rejection tests | `standard_database.rs` V5 invoke and `json_v5_reopen.rs::v5_json_encode_survives_reopen_with_exact_retained_identity` are Compose-gated `#[ignore]` |

| Editor integrations and LSP protocol surface (work ADR 0059, `docs/decisions/0059-offline-lsp-editor-tooling.md`) | `scripts/check-editor-tooling.py`, `justfile`, `editors/tree-sitter-orna`, `editors/textmate`, `editors/vscode`, `editors/neovim`, `editors/vim`, `editors/helix`, `editors/zed`, `editors/emacs`, `crates/orna-lsp` | `python3 scripts/check-editor-tooling.py` passed on 2026-08-27: static editor metadata, generated grammar, tree-sitter accepted corpus, Zed ORDER BY captures, TextMate/Vim/Emacs parity, VS Code checks, Helix configuration, and LSP protocol checks; the gate parsed and validated 49 `.orna` files | Static evidence is current; host sessions are unavailable/unrun (`nvim` is absent; `/usr/bin/vi` is Vim 9.2 Tiny without `-syntax` or `-channel`), so filetype-only smoke does not establish Neovim/Vim runtime parity. Manual Zed/VSIX runtime parity and GUI launch are not claimed |

The current nested source contains the following proof work. Focused results
recorded above are current; workspace reports remain historical and were not
rerun, while the static editor-tooling gate was rerun on 2026-08-27.
Installed evaluator/Inspector-recursion, Compose/Docker, clean-host, live REF,
same-major PostgreSQL, and contract gates remain pending or unclaimed. `nvim` is
absent; `/usr/bin/vi` is Vim 9.2 Tiny without `-syntax` or `-channel`, so its
filetype-only smoke is not host-session proof. Manual Zed/VSIX runtime parity
remains unclaimed:

- same-revision terminal replacement retention across staged CLIENT evaluator
  errors: same_revision_terminal_replacement_persists_when_new_evaluation_fails
  and same_revision_terminal_replacement_persists_when_later_expression_fails;
  both focused checks are current; broader lifecycle proof remains pending;
- cancellation identity and executor ownership checks: validate_owned_request
  and mismatched-identity lifecycle coverage;
- buffered raw CALL_CANCEL handling during preflight:
  buffered_sealed_cancel_prevents_acceptance_for_all_preflight_outcomes and
  buffered_cancel_precedes_first_finish_poll_but_finish_still_runs; the first
  focused check is current and the companion check remains pending;
- resource assignment and AWAIT source spans:
  accepts_procedural_scalar_resource_local_await and
  discovers_stream_resource_target_with_resolved_element_type; both focused
  checks are current; broader compiler proof remains pending;
- accepted no-`STATE` CLIENT procedural local/assignment/`RETURN`
  source-dogfood at
  `crates/orna-server/tests/fixtures/client_local_assignment_dogfood.orna`,
  with offline check
  `checks_and_evaluates_accepted_client_local_assignment_fixture_offline`;
  its focused offline check is current; workspace validation and installed/Compose proof remain pending;
- LSP UTF-16 position and semantic-token coverage, including
  `orna-lsp/tests/lsp_e2e.rs::serves_accepted_client_semantic_tokens_with_utf16_and_nested_ranges`;
  its focused check is current; the static editor-tooling gate above is current,
  while Neovim/Vim host-session and manual Zed/VSIX runtime proof remain pending
  or unclaimed;
- focused positive V3-pinned and V5-pinned standard-library upgrade checks:
  `prepares_the_v3_to_v4_standard_upgrade_from_an_empty_v3_active_revision`
  and `prepares_the_v5_to_v6_standard_upgrade_from_an_empty_v5_active_revision`;
  both focused checks are current; installed/Compose proof remains pending;
- SecurityAdmin privilege filtering for inactive principals and object-scoped
  INSPECT requests (`security_admin_live.rs::prove_has_privilege_filters`);
- raw sealed-invocation queued terminal/cancellation handling, including
  `dispatch_completion_has_claimed_terminal` and the buffered cancellation
  checks in `crates/orna-server/src/raw_socket.rs`;
- trusted audit-recovery search-path establishment before loading audit rows;
  implementation is present, but direct recovery proof remains Compose-gated
  and pending;
- compiler/editor proof work for ordinary CLIENT calls, REF-field
  concatenation, accepted resource/action fixtures, and LSP protocol cases.

Run the remaining lifecycle, raw-preflight, compiler, Compose/Docker, and
clean-host checks before extending the current validated result.

## Historical 2026-08-24 hardening evidence (not current validation)

- `crates/orna-protocol/src/frame.rs::request_id_in_use` scans only bounded
  active streams and terminal tombstones; the prior report records the request-history eviction boundary
  regression; this is not current validation.
- `crates/orna-server/src/raw_socket.rs::dispatch_completion_has_claimed_terminal`
  protects committed sealed `InvokeEvents` terminal records from queued raw-call
  cancellation while preserving replacement for non-terminal batches.
- `crates/orna-client/src/lib.rs::ClientResource::invalidate_with_executor`
  validates executor cancellation completions against the active revision,
  target, declared type, and runtime value before state mutation.
- `crates/orna-client/src/lib.rs::resource_stream_invalidation_abandons_nonterminal_values_before_invalidation`
  and `resource_stream_invalidation_keeps_state_when_abandon_fails` prove
  `StreamValues` cancellation validation, abandon ordering, and unchanged
  state on failed release.
- `crates/orna-client/src/lib.rs::nested_action_stream_values_retain_pending_request`
  proves matching non-terminal stream completions remain owned by the nested
  CLIENT action adapter across both execute and cancel paths.
- `crates/orna-artifact/src/client_plan.rs`, `crates/orna-core/src/value.rs`,
  and `crates/orna-client/src/lib.rs` reject zero action identities in both
  decoder and encoder paths before typed descriptor construction or payload
  assembly.
- `crates/orna-server/src/source_apply.rs` and
  `crates/orna-server/tests/source_apply.rs` keep submitted paths and private
  source-read details out of public source-apply errors; the prior report records a four-test
  integration suite result; this is not current validation.

## Deferred surfaces and blockers

| Surface | Status and evidence boundary |
|---|---|
| Compose/Docker integration matrix | Current `just kernel-test` run passed the ignored server and PostgreSQL library/integration suites against per-test databases and the dedicated empty `ornadb_kernel_gate`; no packaged Debian or clean-host result is claimed. |
| Fresh network-disabled Debian 12 amd64 host or VM proof | Pending environment evidence; Docker, a host PostgreSQL installation, and a second PostgreSQL executable are explicitly disallowed. |
| Neovim/Vim runtime checks | Pending environment evidence; `nvim` is absent and `/usr/bin/vi` is Vim 9.2 Tiny without `-syntax` or `-channel`, so its filetype-only smoke does not establish runtime parity. |
| Live REF field-path evaluation | Pending accepted-runtime seam; the current CLIENT runtime has no trusted object-loader seam, so no live REF result is claimed. |
| Same-major PostgreSQL predecessor transition | Pending contract and implementation; the current-engine no-op and fail-closed unsupported-engine boundary are accepted. |
| Deferred contract gates and unrun accepted proofs | Pending contract or environment gate; accepted bounded Qt v1 ABI/provider/package, populated Inspector resource/UI/presentation/runtime projections, V8 materialised Rows/table presentation, and V9 structural UI constructors have implementation slices. Installed package selection, release baseline, clean-host proof, V8/V9 continuation-specific persistence/recovery proof, full Studio, models/actions/events/layout, reflective JSON-RPC/MCP gateways, remote transport, and general Rows/object-value semantics remain unclaimed or deferred. |
