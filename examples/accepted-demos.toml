# The accepted demo catalogue is intentionally empty.
#
# The former entries all referenced crates/orna-server/tests/fixtures/*.orna.
# Those fixtures are consumed only by the legacy orna-server tests and use the
# historical SERVER/CLIENT surface. The current workspace excludes orna-server;
# frozen v1 callers instead consume the canonical reference/Orna-1.0.0 corpus
# through orna-conformance-v1 and orna-cli-v1. No frozen v1 consumer exists for
# any former entry, so retaining one would claim a runnable proof that cannot
# be reached from the current product graph.
#
# Add an entry only with a frozen v1 source, a direct v1 consumer, and a focused
# proof command. Do not repoint legacy fixtures here as a compatibility shim.
